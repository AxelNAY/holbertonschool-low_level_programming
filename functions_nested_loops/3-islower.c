#include <unistd.h>
#include "main.h"

/**
* _islower - return 1 if c is lowercase and 0 otherwise,
* followed by a new line.
*/

int _islower(int c)
{
if (_islower(c))
return (1);
else
return (0);
}
