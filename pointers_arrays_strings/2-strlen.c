#include "main.h"
#include <stdio.h>

/**
* _strlen - return the length of the pointer
*
* @s: pointer s
*
* Return: return the length
*/

int _strlen(char *s)
{
int length = 0;
while (*str++)
length++;
return (length);
}
