#include "main.h"
#include <stdio.h>

/**
* cap_string - capitalizes all words of a string.
*
* @c: pointer c
*
* Return: result
*/

char *cap_string(char *c);
{
char sep[13] = {' ', '\t', '\n', ',', ';', '.', '!', '?', '"', '(', ')', '{', '}'};

return(*c);
}
