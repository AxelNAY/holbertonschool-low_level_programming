#include "main.h"
#include <stdio.h>

/**
* _isupper - checks numbers
*
* @c: Character to analyse.
*
* Return: 1 if success or 0 otherwise.
*/

int _isdigit(int c)
{
if ((c >= 48) && (c <= 57))
return (1);
else
return (0);
}
